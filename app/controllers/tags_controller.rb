class TagsController < ApplicationController

  # def most_popular
  #   all_tag_name = PictureTag.all.map {|pictag| pictag.tag}
  #   count_tags = all_tag_name.each_with_object(Hash.new(0)) { |name,counts| counts[name] += 1 }
  #   ordered_tags = count_tags.sort_by {|key, value| value}.reverse
  #   ordered_tags[(0..2)]
  #   render
  # end

  # def trending
  #
  # end
end
